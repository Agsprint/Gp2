<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.usst.dao.TeacherMapper">
    <!--登录查询-->
    <select id="checkUser" resultType="teacher">
        select * from t_teacher where name=#{name} and password=#{pwd};
    </select>

</mapper>